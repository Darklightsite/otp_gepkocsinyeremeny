# 🚗 OTP Gépkocsinyeremény - Egyszerű Kártya
# ⚠️ Cseréld le az entitás neveket ha más nevet adtál az integrációnak!

type: custom:mushroom-template-card
primary: >-
  {% if states('sensor.otp_betetek')|int(0) > 0 %}
    🏆 {{ states('sensor.otp_betetek') }} db nyeremény!
  {% else %}
    🚗 OTP Gépkocsinyeremény
  {% endif %}
secondary: >-
  {% set count = states('sensor.otp_betetek')|int(0) %}
  {% if count > 0 %}
    {% set items = state_attr('sensor.otp_betetek', 'nyertes_reszletek') %}
    {% if items %}{{ items[0].szam }}{% if items|length > 1 %}, {{ items[1].szam }}{% endif %}{% endif %}
  {% else %}
    {{ state_attr('sensor.otp_betetek', 'figyelt_db') | default(0) }} betét
  {% endif %}
icon: >-
  {% if states('sensor.otp_betetek')|int(0) > 0 %}mdi:trophy{% else %}mdi:car-convertible{% endif %}
icon_color: >-
  {% if states('sensor.otp_betetek')|int(0) > 0 %}amber{% else %}blue{% endif %}
layout: horizontal
tap_action:
  action: more-info
  entity: sensor.otp_betetek
hold_action:
  action: call-service
  service: button.press
  target:
    entity_id: button.otp_betetek_adatbazis_frissitese
